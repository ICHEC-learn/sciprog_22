./Fibonacci
this program takes an integer from user as input and returns the fibonacci series of that length.

./Scan
just taking an integer and float from user as input.


./Arctan
this program takes an integer as input and compute the Maclaurin series using two different arctanh approximation approaches, with given precision specified by user.
Then the results are compared 